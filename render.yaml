services:
  - type: web
    name: marvan-aegis-quant
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py --mode run
    envVars:
      - key: PORT
        value: 8000
      - key: PYTHON_VERSION
        value: 3.9.0
